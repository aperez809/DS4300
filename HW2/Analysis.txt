HW 2 Analysis 

Strategy 1:

  Posting Tweets using this strategy loads at about 9000 tweeets per second. 
  
  
Strategy 2:
  Posting tweets using the second strategy is slightly slower as it automatically looks to add the tweets to the timeline. This Process currently takes 200 seconds for all 1M tweets or 5000 tweets per second. However to load the home timeline using this strategy takes under a second.
